<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pdi.projetopdi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.EditarProduto"></activity>
        <activity android:name=".ui.activity.ListaPedidos" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.NovoProduto" />
        <activity android:name=".ui.activity.ListaProdutosActivity"/>
        <activity android:name=".ui.activity.PrincipalActivity" />
        <activity android:name=".ui.activity.LoginActivity"/>
    </application>

</manifest>